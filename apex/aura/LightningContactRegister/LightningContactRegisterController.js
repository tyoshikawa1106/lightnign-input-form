({
    changePageType : function(component, event, helper) {
        // レコードID変更
        helper.changeRecordId(component, event);
        // ページタイプ切り替え
        helper.changePageType(component, event);
    }
})